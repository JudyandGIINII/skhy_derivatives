# prd.md 11.2 기본 한도. 비율은 백분율이 아닌 fraction으로 기록한다.
policy_version: prd-11.2-v1
max_risk_per_trade_fraction: "0.0025"
max_daily_loss_fraction: "0.01"
max_cumulative_mdd_fraction: "0.05"
h1_quote_max_age_seconds: "2"
h2h3_quote_max_age_seconds: "5"
leg_timeout_seconds: "5"

# G-02 — KIS·Toss capability·한도·token

| 필드 | 내용 |
| --- | --- |
| 상태 | `CONFIRMED` |
| 질문 | KIS·Toss의 계정별 실시간 시장·세션·필드 범위, 호출·구독 한도, token 수명 |
| 기능 범위 | `MarketDataProvider` capability catalog 중 조회 전용(KRX 일별시세, KIS 국내현재가, Toss 종목기준정보) |
| 공식 URL/문서 버전 | https://apiportal.koreainvestment.com/apiservice-category , https://developers.tossinvest.com/llms.txt |
| 계정·시장 범위 | 사용자 조회 전용 계정(KIS APP_KEY/SECRET, Toss CLIENT_ID/SECRET, KRX API_KEY). 계정별 상이 가능하므로 타 계정에 일반화 금지 |
| 확인시각 | 2026-07-18T09:57:26Z |
| 원본 증거 checksum | `c11c6779bbeffd02efb6b900602fdfa15f68c6db57b39632b9ae0ed109c82b91` (SHA-256, `docs/decisions/gates/evidence/G-02-capability-probe.json`) |
| 담당 provider | 한국투자증권(KIS), 토스증권 |
| 결론 | 세 공급자 read-only capability probe 실제 응답 확인. KRX 일별종목시세(`/svc/apis/sto/stk_bydd_trd`, 15필드, 944레코드), KIS 국내현재가(`/uapi/domestic-stock/v1/quotations/inquire-price`, 80필드), Toss 종목정보(`/api/v1/stocks`, 14필드) 조회 성공. 필수 필드(`BAS_DD`/`stck_prpr`/`symbol`) 관측. token은 실측 응답 기반으로 KIS 24h 유효·6h 조기갱신 적용 |
| 적용 config | `ProviderCatalogEntry.capabilities`를 문서 주장이 아닌 위 실측 probe 결과로 채운다 |
| 유효기간·재확인 조건 | 2026-10-16T09:57:26Z까지 유효(90일). API 버전 변경 또는 만료 시 재확인 |
| 미확인 시 기본동작 | 지원 기능으로 가정하지 않음(`UnsupportedCapabilityError`) |

## 이력

- (초기 등록) 상태 `UNKNOWN`. Phase 0 완료 전 실제 조회 전용 키로 capability probe
  실행 필요 (`application.capability_probe.run_capability_probe`).
- 2026-07-18 상태 `CONFIRMED`. macOS Keychain(`SKHY_SECRET_BACKEND=keychain`)에
  주입된 조회 전용 키로 `tests/e2e/test_provider_smoke.py` 3건 통과 및 capability
  probe 실측. 증거는 `evidence/G-02-capability-probe.json`(위 checksum)로 고정.
  재검증: `shasum -a 256 docs/decisions/gates/evidence/G-02-capability-probe.json`.

계정별 capability와 대차(borrow) 조건은 다른 계정에 일반화하지 않는다.
